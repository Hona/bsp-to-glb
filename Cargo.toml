[package]
name = "bsp-to-glb"
version = "0.1.0"
edition = "2024"

[dependencies]
base64 = "0.22"
lzma-rs = "0.3"
pathdiff = "0.2"
png = "0.18"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
zip = { version = "2", default-features = false, features = ["deflate"] }
